services:
  - type: web
    name: mini-blog
    env: python
    startCommand: gunicorn --chdir /path/to/app app:app
    buildCommand: pip install flask python-dotenv secure-smtplib
    plan: free

cronJobs:
